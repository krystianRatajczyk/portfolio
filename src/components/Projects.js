import React from 'react'
import styles from '../style'

const Projects = () => {
  return (
    <div className={`${styles.flexCenter} ${styles.paddingXSection} w-[100vw] h-[100vh] border`} id="projects">Projects</div>
  )
}

export default Projects